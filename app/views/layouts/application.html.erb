<!DOCTYPE html>
<html>
  <head>
    <title>Eventdim</title>
    <link rel="icon" type="image/x-icon" href="https://eventdim-seeds.s3.us-west-1.amazonaws.com/eventbrite.png">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <% if current_user %>
      <script>
        window.currentUser = {
          "id": <%= current_user.id %>,
          "email": "<%= current_user.email %>"
        };
      </script>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
